pipeline {
    agent any
    
    stages {
        stage('Test') {
            steps {
                echo 'Hello, Jenkins!'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
